Review the current watchlist. Execute: `uv run tradekit watchlist $ARGUMENTS`

If no arguments, analyze the default watchlist.
If arguments provided (e.g. --name momentum), analyze that specific watchlist.

For each ticker, provide a 1-line summary: price, gap%, trend direction, and whether it's actionable today. Highlight any that are setting up for a potential trade.
