// Hand-written — NOT generated. Listed in HAND_WRITTEN in
// scripts/gen-mobile-routes.ts so the generator leaves it alone.
//
// Desktop /telos stacks all twelve sections into one scroll; on a phone that
// is too much page, so mobile lands on the section index and loads one
// section per route from the shared registry.
export { default } from "@/components/mobile/TelosIndex";
