# Test configuration for mai-network-skill
# Uses mock adapter for testing

version: "1.0"
domains:
  network:
    primary: mock
    adapters:
      mock:
        devices: 3
        clientsPerDevice: 5
