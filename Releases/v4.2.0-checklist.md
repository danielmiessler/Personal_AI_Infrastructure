# v4.2.0 Development Checklist

Branch: `v4.2.0`
Milestone: https://github.com/DevenDucommun/Personal_AI_Infrastructure/milestone/1

---

## 1. Atomic writes for JSON state files ✅

**Already atomic (no work needed):**
- [x] `SessionAutoName` — `session-names.json` (pid-based tmp + renameSync)
- [x] `prd-utils.ts` — `work.json` (tmp + renameSync)

**Converted to atomic:**
- [x] Create `hooks/lib/atomic.ts` — `atomicWriteJSON(path, data)` + `atomicWriteText`
- [x] `SessionCleanup.hook.ts` — session-names.json + last-cleanup.json
- [x] `handlers/SystemIntegrity.ts` — integrity state JSON
- [x] `handlers/DocCrossRefIntegrity.ts` — review queue + drift state JSON
- [x] `handlers/UpdateCounts.ts` — usage cache + settings.json
- [x] `lib/tab-setter.ts` — tab state JSON files
- [x] Tests: `AtomicWrite.test.ts` (9 tests)

---

## 2. Algorithm micro-effort tier ✅

- [x] Add `Micro` tier to `PAI/Algorithm/v3.8.0.md` → bumped to `v3.9.0`
  - Budget: <30s | ISC: 1–4 | Min Capabilities: 0
  - Single bounded change, 1-3 files, no design decisions
  - Handled inline in CLAUDE.md — never loads Algorithm file
- [x] Update `CLAUDE.md` — effort tier pre-classification + inline Micro format
- [x] Lazy-load: Micro uses inline format; Standard+ loads `v3.9.0.md`
- [x] Updated `hooks/lib/recovery-block.ts` — bumped to v3.9.0
- [x] Updated `tests/PostCompactRecovery.test.ts` — v3.9.0 version assertion

---

## 3. Hook payload validation (Zod)

- [ ] Add `zod` to project (or use bun-native validation)
- [ ] Create `hooks/lib/payload-schema.ts` — schemas for all hook events:
  - `UserPromptSubmit`, `Stop`, `SessionStart`, `SessionEnd`, `PreToolUse`, `PostToolUse`
- [ ] Update `hooks/lib/hook-io.ts` — validate parsed payload, log warnings on mismatch
- [ ] Write tests for schema validation (valid + invalid payloads)

---

## 4. Split monolithic settings.json

- [ ] Create `config/identity.jsonc`
- [ ] Create `config/hooks.jsonc`
- [ ] Create `config/permissions.jsonc`
- [ ] Create `config/notifications.jsonc`
- [ ] Create `config/preferences.jsonc`
- [ ] Create `BuildSettings.ts` — merges config/ → settings.json with validation
- [ ] Replace `_docs` fields with JSONC comments
- [ ] Update `install.sh` to run BuildSettings on install
- [ ] Write tests for BuildSettings merge + validation

---

## 5. Upgrade CLI (`pai upgrade`)

- [ ] Create `manifest.json` for v4.2.0 (file list + checksums)
- [ ] Create `pai.ts` CLI entry point
- [ ] Implement `upgrade` command:
  - [ ] Auto-backup current `~/.claude` → `~/.claude-backup-{date}`
  - [ ] Integrity validation against manifest.json
  - [ ] Dependency check (Bun version, Claude Code presence)
  - [ ] Atomic swap (current → `.old`, staging → current)
  - [ ] Post-upgrade migration hooks
  - [ ] Auto-rollback on failure
- [ ] Write tests for backup, integrity check, rollback

---

## Release tasks

- [ ] Create `Releases/v4.2.0/` snapshot from updated v4.1.0 files
- [ ] Write `Releases/v4.2.0/CHANGELOG.md`
- [ ] Update `Releases/v4.2.0/README.md`
- [ ] Run full test suite — all pass
- [ ] Commit, push, PR → merge to fork main
